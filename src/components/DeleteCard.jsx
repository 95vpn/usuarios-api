import React, { useState } from 'react'
import CardUser from './CardUser'

const DeleteCard = ({ user }) => {
    
  return (
    <div>
        <h3>dfdfffds{user.first_name} {user.last_name}</h3>
    </div>
  )
}

export default DeleteCard